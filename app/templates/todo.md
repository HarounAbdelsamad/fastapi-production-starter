# TODO - `app/templates/`

Template assets used by email/render pipelines belong here.

## How to add templates
- Group templates by domain (`email/`, `reports/`, etc.).
- Keep variables explicit and documented in sender code.

## Rule
- Never hardcode secrets or sensitive values in templates.
