MANUAL STEPS TO COMPLETE — fastapi-production-starter
======================================================
Covers every remaining human action from the plan's shippability checklist
(§9.1–§9.6) through launch and first 90 days.
Delete this file when all boxes are checked.

Legend:
  [ ] = not started
  [~] = in progress
  [x] = done (automated this session)
  [M] = done manually by you

======================================================================
SECTION A — PRE-PUBLISH VERIFICATION
======================================================================

[x] A1. Run the full test suite — passes 65% threshold (currently 65.21%)
        Fixed: asyncio_default_fixture_loop_scope = "session" in pyproject.toml,
               String(255) on all MySQL models, engine reset per test in conftest.

[x] A2. Lint clean — ruff check + ruff format --check both pass.

[x] A3. mypy baseline clean — all type errors fixed with targeted # type: ignore.

[ ] A4. Skim the OpenAPI docs at http://localhost:8000/docs after `make run`
        Check: every endpoint has a summary, key endpoints have response examples.
        At minimum: /auth/login, /health/ready, /api/v1/users/me, /api/v1/webhooks/

======================================================================
SECTION B — GIT & GITHUB SETUP
======================================================================

[x] B1. All changes committed and pushed to master.
        Latest commit fixes CI (event loop isolation, MySQL VARCHAR, security scan).

[M] B2. GitHub repo metadata set (description, topics, website URL).

[x] B3. GitHub Pages enabled — source set to GitHub Actions.
        Docs workflow triggered and deployed.

[ ] B4. Verify docs site is live
        Open: https://harounabdelsamad.github.io/fastapi-production-starter/
        Check: search works, Mermaid diagrams render, light/dark toggle works.

[x] B5. CI matrix is green — all 5 jobs pass on latest push (run 26255115259):
          Lint ✓  |  Type Check (mypy) ✓  |  SQLite ✓  |  PostgreSQL ✓  |  MySQL ✓
        Note: badge URL updated to ?branch=master to fix stale CDN cache.

[x] B6. Tagged v0.1.0 — triggered multi-arch Docker build to GHCR.

[x] B7. GitHub release v0.1.0 created with release notes.

[ ] B8. Verify Docker image built and is public on GHCR
        The Docker build for v0.1.0 tag failed (separate workflow issue).
        Check: https://github.com/HarounAbdelsamad?tab=packages
        If image is missing or private, re-trigger:
          git tag -d v0.1.0 && git push origin :v0.1.0
          git tag v0.1.0 && git push origin v0.1.0
        Then make image public in package settings if needed.

[ ] B9. Pin repository on your GitHub profile
        github.com/HarounAbdelsamad → Edit profile → Customize pins → add this repo
        (Cannot be done via API — browser only.)

[M] B10. GitHub Discussions enabled.
         Create first pinned discussion: "Welcome — share your use case"

======================================================================
SECTION C — REFERENCE IMPLEMENTATION (public live demo)
======================================================================

[x] C1. Separate public GitHub repo created: fastapi-pt-demo
        https://github.com/HarounAbdelsamad/fastapi-pt-demo

[x] C2. Scaffold (models, schemas, services, router, main.py) pushed to
        https://github.com/HarounAbdelsamad/fastapi-pt-demo

[ ] C3. Deploy it to a free tier host
        Recommended options (all have free tiers):
          - Fly.io:   fly launch (auto-detects Dockerfile) → free hobby plan
          - Railway:  connect GitHub repo → deploy
          - Render:   new Web Service → link repo → free tier
        Use SQLite for the demo deployment (no extra DB service needed).

[ ] C4. Update README.md with live demo URL
        Add a "Reference Implementation" section with:
          - Link to the demo repo (https://github.com/HarounAbdelsamad/fastapi-pt-demo)
          - Live URL (once deployed)
          - One-paragraph description of what it demonstrates

======================================================================
SECTION D — EXTERNAL REVIEW (required before public announcement)
======================================================================

[ ] D1. Identify 2 reviewers
        Good sources:
          - Ex-colleagues from Samsung or previous roles
          - FastAPI GitHub contributors
            (github.com/fastapi/fastapi/graphs/contributors)
          - Active members of German Python community
            (pythonde.slack.com or PyMunich / PyBerlin meetup attendees)

[ ] D2. Send review request (copy-paste message below)
        -----------------------------------------------------------------
        Subject: Senior eye on an enterprise FastAPI foundation?

        I've spent the past several months building an enterprise-grade
        FastAPI foundation — SAML 2.0 SSO, multi-DB CI matrix, pluggable
        secrets (Vault/AWS), OpenTelemetry wired in, HMAC-signed audit
        logs, GDPR helpers, circuit breakers, outgoing webhooks — all
        documented in 12 Architecture Decision Records.

        Would value a senior eye before I announce it publicly. One hour
        of your time, honest feedback only — no obligation to be positive.

        Repo: https://github.com/HarounAbdelsamad/fastapi-production-starter
        Docs: https://harounabdelsamad.github.io/fastapi-production-starter/
        -----------------------------------------------------------------

[ ] D3. Ask reviewers to specifically check:
          - README: does it pass the "would I show this to a CTO" test?
          - SECURITY.md: is the OWASP table accurate and honest?
          - app/core/auth.py, app/core/apikey.py: any obvious auth flaws?
          - app/core/saml.py: any SAML assertion validation gaps?
          - docs/identity/ sequence diagrams: are the flows correct?

[ ] D4. Act on feedback — fix everything flagged as an actual issue
        (not every opinion needs to be addressed, but security/accuracy issues do)

======================================================================
SECTION E — LAUNCH POSTS (do after D4, in this exact order)
======================================================================

[ ] E1. LinkedIn post — CV amplification, not a promo post
        Frame: "I spent 6 months solving the patterns every enterprise team
        re-implements from scratch. Here's what I built and why."
        Structure:
          Para 1: The problem (SAML, Vault, zero-downtime migrations —
                  patterns re-written at every company)
          Para 2: What you built (the 12 ADRs, the OWASP table, the CI matrix)
          Para 3: Link to docs site + call for feedback
        DO NOT lead with "I built a FastAPI template" — that's generic.
        Lead with the enterprise problem you're solving.

[ ] E2. Show HN post — post while LinkedIn is getting traction
        Title: "Show HN: Enterprise FastAPI foundation – SAML, multi-DB,
                OTel, HMAC audit logs, 12 ADRs"
        Opening paragraph for the comment thread:
          "Most FastAPI starters assume you're building a SaaS product.
           This assumes you're building something that will be scrutinized by
           an enterprise security team. Every feature is off by default.
           All 12 design decisions are documented as ADRs."
        Best time to post: Tuesday–Thursday, 9am–11am US Eastern.

[ ] E3. Blog post — pick ONE of these topics and write 800–1200 words:
        Option A: "Why we HMAC-sign every audit log row (and why you should too)"
        Option B: "Testing FastAPI on 3 databases in CI: what breaks and why"
        Option C: "The async circuit breaker in Python: three states, one asyncio.Lock"
        Submit to: ITNEXT (Medium) or Better Programming (Medium)
        Cross-post to: dev.to and your own Hashnode/blog
        Add link from README docs section once published.

[ ] E4. Reddit posts (after E2 and E3 are up)
        r/Python: link to Show HN thread (consolidates comments)
        r/FastAPI: direct post with brief description
        r/programming: only if the blog post is exceptional

[ ] E5. Targeted outreach — 10-person list (do over 2 weeks, not all at once)
        Who to reach:
          - FastAPI maintainers (@tiangolo and core contributors)
          - Authors of FastAPI books/courses
          - DevRel engineers at Sentry, Datadog, Honeycomb
            (they link to projects that demonstrate their product well)
          - Enterprise-focused Python newsletters
        Message: 3 sentences max. "Built X. Thought it might be relevant
        to your work on Y. No ask." Link only. No reply needed.

======================================================================
SECTION F — FIRST 90 DAYS POST-LAUNCH (recurring cadence)
======================================================================

Weekly:
  [ ] F1. Review and merge Dependabot PRs
  [ ] F2. Acknowledge any new issues within 48 hours
           (even a "looking into this" counts — silence kills trust)

Monthly:
  [ ] F3. Cut a tagged release (v0.1.1, v0.1.2, etc.)
           Even if it's only dependency bumps — release cadence signals health.
  [ ] F4. Publish one blog post on an architectural decision

Quarterly:
  [ ] F5. Update README.md roadmap section
  [ ] F6. Pin a "Current focus" issue on the repo
  [ ] F7. Review and close stale issues

Conference CFPs (submit as soon as they open — track at papercall.io):
  [ ] F8. PyCon DE       — easiest, you're in Germany; usually opens Oct/Nov
  [ ] F9. EuroPython     — usually CFP opens Jan/Feb
  [ ] F10. FastAPI Conf  — directly relevant audience
  [ ] F11. PyCon US      — long shot but worth a submission

  Talk title options:
    "Production patterns every team rewrites — and how to stop"
    "FastAPI at enterprise scale: SAML, multi-tenancy, observability"
    "12 architectural decisions, packaged: an opinionated FastAPI foundation"
    "Zero-downtime database migrations in FastAPI: the expand-contract playbook"

======================================================================
DONE WHEN (hard gates — don't announce until all are checked):
======================================================================

  [x] A: Tests pass, lint clean, mypy baseline reviewed
  [x] B1-B5: Pushed, CI green, docs site live
  [~] B6-B8: v0.1.0 tagged ✓, release notes ✓, Docker image — needs verify (B8)
  [ ] C: Reference implementation live at a public URL (C3 + C4 pending)
  [ ] D: 2 external reviewers gave feedback; critical issues fixed
  [ ] E1-E3: LinkedIn + Show HN + first blog post published

After those 6 lines are checked — the project is done.
Everything in F is ongoing maintenance, not a launch gate.
======================================================================
